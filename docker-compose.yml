version: "3.9"

services:
  frontend:
    build: .
    container_name: superwincasino-frontend
    ports:
      - "80:80"
    environment:
      VITE_CONVEX_URL: ${CONVEX_URL}
      VITE_FILE_STORAGE_URL: ${FILE_STORAGE_URL}
    restart: always
